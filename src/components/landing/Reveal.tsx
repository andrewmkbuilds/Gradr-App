/**
 * Landing reveal — thin alias over the shared motion primitive so every
 * surface uses one entrance language.
 */
export { Reveal, default } from "@/components/motion/Reveal";
export type { RevealProps } from "@/components/motion/Reveal";
