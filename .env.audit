# Mode file used only by the vertical-spacing audit (`bun run audit:spacing`).
# The deployed app pins a single surface; the audit needs the multi-surface
# router so every public marketing/tool route renders at its path prefix
# instead of redirecting to the app surface.
VITE_GRADR_SURFACE=
VITE_APP_SUBDOMAIN_LIVE=false
